# Claude Code project instructions

Read and follow `AGENTS.md` as the canonical project instructions.

Use `C:\Users\Andres\Documents\Projects\app-finanzas` as the shared checkout
with Codex. Do not create or use another Git worktree unless the user explicitly
requests isolation. Use regular branches in this checkout, publish completed
work to `origin/main`, and return the checkout to an up-to-date `main` when the
task is closed.
