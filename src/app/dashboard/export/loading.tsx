import { PageLoading } from '@/components/page-loading'

export default function ExportLoading() {
  return (
    <PageLoading
      title="Export CSV"
      description="Loading export options for this household."
    />
  )
}
