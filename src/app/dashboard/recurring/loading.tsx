import { PageLoading } from '@/components/page-loading'

export default function RecurringLoading() {
  return (
    <PageLoading
      title="Recurring"
      description="Loading recurring transaction templates."
    />
  )
}
